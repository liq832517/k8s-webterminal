server:
  port: 22222
  session:
    timeout: 10
  tomcat:
    uri-encoding: UTF-8
spring:
  datasource:
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3036/koe
  mvc:
    static-path-pattern: /**

kubernetes:
  config:
    config-path: /Users/liqi/.kube/config
